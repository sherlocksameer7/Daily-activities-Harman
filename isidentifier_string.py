inp = input("Enter any input: ")

out = inp.isidentifier()

print(out) #  it totally denotes that it is identifying only string or not as also be checking the spaces we made while giving an input.

# ( istitle() ) in this method we also using for determine the string only.